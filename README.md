# Wavely Store

React + Vite storefront for Wavely (dropshipping). Run locally with `npm install` and `npm run dev`. Production build: `npm run build`.

Repository: https://github.com/eyadahmed1-eltomy/wavely-store
